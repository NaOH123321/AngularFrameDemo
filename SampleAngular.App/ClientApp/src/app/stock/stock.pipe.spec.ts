/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StockPipe } from './stock.pipe';

describe('Pipe: Stocke', () => {
  it('create an instance', () => {
    let pipe = new StockPipe();
    expect(pipe).toBeTruthy();
  });
});
